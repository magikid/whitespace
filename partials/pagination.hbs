<div class="pagination">
  {{#if prev}}   
    <a class="prev" href="{{page_url prev}}">&larr; Older</a>
  {{/if}}
  
  <span class="page-number">Page {{page}} of {{pages}}</span>
  
  {{#if next}}
    <a class="next" href="{{page_url next}}">Newer &rarr;</a>
  {{/if}}
</div>
